// GoldHistory 頁的「非文字」設定。文字在 locales/*.json 的 `goldHistory` 命名空間。
// 此頁僅 mid-section 博物館段有一張 PhImg；ERAS / MED_ERAS 純文字無圖，由 locale t.raw 取陣列。

export const GOLD_HISTORY_MUSEUM_IMG = {
  kw: 'goldmine,mountain,heritage',
  lock: 73,
  img: '/images/gold-history/museum.jpg',
};
